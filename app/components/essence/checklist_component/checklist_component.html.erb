<div <%= tag.attributes **html_options %>>
  <h4 class="checklist-title"><%= title %></h4>
  <ul class="checklist-items">
    <% items.each do |item| %>
      <%= item %>
    <% end %>
  </ul>
</div>
